# ConnectRoom client

React + Vite + Capacitor frontend for ConnectRoom.

**Product README (demo, stack, full setup):** [../README.md](../README.md)

```bash
npm install
npm run dev
npm run build
npm run test:run
```

Production env: `VITE_WS_URL`, `VITE_MEDIA_BASE` (see `.env.production`).
