# Same secret as chat_server (Config/chat_server.env / JWT_SECRET).
# systemd: EnvironmentFile=/root/media_storage/deploy/media-server.env
#
# Local dev until chat_server issues signed URLs on every response:
#   MEDIA_ALLOW_UNSIGNED=1
#
JWT_SECRET=replace-with-same-value-as-chat-server

# MEDIA_ALLOW_UNSIGNED=1
