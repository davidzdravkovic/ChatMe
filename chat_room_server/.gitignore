# build folders (ALL)
build/
build-*/
build*

# Deploy artifacts (machine local)
deploy/

# CMake
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json

# binaries
*.exe
*.obj
*.o
*.a
*.dll
*.pdb

# editor / tooling
.vscode/
.cursor/

# Local learning-scope graph (not part of the chat server product)
ScopeController/

# VPS / local secrets (see deploy/chat-server.env.example)
Config/chat_server.env

# vcpkg local install (large; reproducible from vcpkg.json + manifest)
vcpkg_installed/

# deploy/sync artifacts (generated, machine-local)
*.sha256
local-schema.sql
prod-schema.sql
